services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - cb-index-data:/app/data
      - ./index.html:/app/index.html:ro
    restart: unless-stopped

volumes:
  cb-index-data:
